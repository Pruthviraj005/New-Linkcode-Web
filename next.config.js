/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['aceternity.com', "www.linkcode.in", "images.unsplash.com"],
  },
}

module.exports = nextConfig
